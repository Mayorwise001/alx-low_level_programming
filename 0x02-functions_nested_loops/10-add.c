#include "main.h"
#include <stdio.h>

/**
*add - main function
*
*@i: prints a list of time
* @k: row, cone = column, d = digits of current result
*Return: times table.
*
*/

int add(int i, int k)
{
	return (i + k);
}
