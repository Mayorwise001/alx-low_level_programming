#include "main.h"

/**
* _abs - main function
*
*@n: character to compare
*
*Return: 1 fi positive, 0 if zero, -1 if negative
*
*/

int _abs(int n)

{

	if (n > 0)
		return (n);
	}
	else
		return (n * -1);
}
