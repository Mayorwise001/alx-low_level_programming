#include <stdio.h>

/**
    *main - print base numbers
    *
    *Return: only new numbers
    */

int main(void)
{
int i = 0;

	while (i < 10)
{

		putchar(i);
		i++;
		putchar(10);
}
	return (0);
}
